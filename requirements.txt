# ============================================================================
# PRODUCTION RESEARCH ASSISTANT - DEPENDENCIES
# ============================================================================

# -----------------------------------------------------------------------------
# CONFIGURATION MANAGEMENT
# -----------------------------------------------------------------------------
python-dotenv==1.0.0
# Loads environment variables from .env file
# Why: Keeps secrets out of code, different configs per environment

pydantic==2.5.0
# Data validation using Python type hints
# Why: Type-safe config, catches errors on startup, not runtime

pydantic-settings==2.1.0
# Pydantic extension for settings management from env vars
# Why: Automatic env var loading with validation, no manual parsing

# -----------------------------------------------------------------------------
# LLM INTEGRATION
# -----------------------------------------------------------------------------
groq==0.4.0
# Groq API client for LLM access (Llama 3.3 70B)
# Why: Fast inference, free tier (14,400 requests/day), good quality

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
loguru==0.7.2
# Simple and powerful logging library
# Why: JSON formatting, auto-rotation, request ID tracking, better than stdlib

# -----------------------------------------------------------------------------
# ERROR HANDLING & RETRIES
# -----------------------------------------------------------------------------
tenacity==8.2.3
# Retry library with exponential backoff
# Why: Handles transient LLM failures, prevents cascading errors, circuit breakers

# -----------------------------------------------------------------------------
# WEB FRAMEWORK (API)
# -----------------------------------------------------------------------------
fastapi==0.109.0
# Modern async web framework
# Why: Auto API docs, type validation, async support, fast

uvicorn[standard]==0.27.0
# ASGI server to run FastAPI
# Why: Production-ready server, WebSocket support, auto-reload in dev

httpx==0.27.0
# Async HTTP client (like requests but async)
# Why: Call mock server, external APIs, async-compatible

# -----------------------------------------------------------------------------
# ORCHESTRATION (Multi-Agent Coordination)
# -----------------------------------------------------------------------------
langgraph==0.2.45
# Graph-based agent orchestration
# Why: Manages agent state flow, conditional routing, retry loops

langchain-core==0.1.23
# Core abstractions for LangChain (required by LangGraph)
# Why: Provides base types and interfaces for LangGraph

# -----------------------------------------------------------------------------
# TESTING
# -----------------------------------------------------------------------------
pytest==7.4.3
# Testing framework
# Why: Write and run unit/integration tests, fixtures, parametrization

pytest-asyncio==0.23.3
# Pytest plugin for async testing
# Why: Test async agents and FastAPI endpoints

# -----------------------------------------------------------------------------
# INSTALLATION COMMAND
# -----------------------------------------------------------------------------
# pip install -r requirements.txt